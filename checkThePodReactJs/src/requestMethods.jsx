import { useContext } from "react";
import AuthContext from "./context/AuthProvider";

const BASE_URL = "http://localhost:3500/";

export const fetchFromPublic = (method, headers) => {
  fetch;
};
export const fetchFromUser = () => {};
